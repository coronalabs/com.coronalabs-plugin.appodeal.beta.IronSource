Sources for the plugin `plugin.appodeal.beta.IronSource`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.IronSource" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
